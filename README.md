# Dialysis Dashboard

A full-stack application for managing dialysis patient records and sessions.

## 🚀 Getting Started

### Backend Setup
1. Navigate to the backend directory: `cd backend`
2. Install dependencies: `npm install`
3. Set up the environment variables by creating a `.env` file based on your configuration.
4. Run the server: `npm run dev` or `npm start`
